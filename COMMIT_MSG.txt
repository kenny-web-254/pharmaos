fix: comprehensive QA audit - fix all broken functionality & complete flows

CRITICAL FIXES:
- POS: Complete Sale button now functional with API integration, loading state, error handling
- Inventory: Implemented full CRUD (Add/Edit/Delete modals) + real API data fetching
- Customers: Implemented full CRUD with modals, validation, and API integration
- Suppliers: Created new page with complete CRUD functionality
- Settings: Created new page with org settings, theme, feature toggles
- Register: Created new registration page with form validation
- Currency: Converted all hardcoded USD values to KES (Kenyan Shillings)

NAVIGATION:
- Fixed suppliers route (was missing)
- Fixed settings route (was missing)
- Added all new pages to App.tsx routing

API INTEGRATION:
- Dashboard now fetches real-time data (sales, customers, low stock)
- All pages use actual API endpoints instead of mock data
- Auto-create default branch if missing (fixes POS checkout)
- Fixed auth store to persist selectedBranch from user

UX IMPROVEMENTS:
- Added react-hot-toast globally for success/error feedback
- Loading states on all async actions
- Modal backdrop closes on click
- Real barcode scanner integration (html5-qrcode)
- Form validation on all modals
- Empty states for no data scenarios

BACKEND:
- Fixed organizationController to accept name, email, businessType updates
- Ensures settings persistence from frontend

TECHNICAL:
- Removed unused imports across all files
- Fixed dynamic Tailwind classes that wouldn't compile
- Fixed posStore cart total to apply item discounts correctly
- Fixed Inventory low-stock warning (computed from minStockLevel)
- Fixed Login/Register to fetch full user with branch after auth

All major user flows now fully functional: registration → onboarding → dashboard,
inventory management, POS checkout, customer/supplier management, settings persistence.
