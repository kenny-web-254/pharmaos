fix: build syntax errors - missing fragment closing and duplicate function declarations

- Fixed InventoryPage.tsx: corrected JSX structure in product table rows
  - Properly closed </motion.tr> tags
  - Fixed tbody/map syntax from `))}` to `)}`

- Fixed POSPage.tsx: removed duplicate function declarations
  - Removed duplicate handleBarcodeScan and handleScanSuccess definitions
  - Removed unused scannedProduct state
  - Fixed extra closing brace at end of file

- Fixed Sidebar.tsx: added missing fragment closing tag `</>`
  - Fragment opened with `<>` but never closed, causing parse error

These syntax errors prevented the Vite build from succeeding.
Now the frontend compiles cleanly and is ready for Vercel deployment.
